// Replace method => searches a value in a string & and return a new str with the replace value
let str = "ibrahim";
let newStr = str.replace("ibrahim","butt");
document.write(newStr);

document.write("<br>");
// Repeat method => Returns a number of copies of a string

let newStr1 = str.repeat(3);
document.write(newStr1);
